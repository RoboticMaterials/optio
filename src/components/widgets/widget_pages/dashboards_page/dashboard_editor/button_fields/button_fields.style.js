import styled from "styled-components";

export const SchemaIcon = styled.i`
    color: ${props => props.color};
    background: white;
    padding: .25rem;
    font-size: 1rem;
    margin: 0;
    height: fit-content;
    width: fit-content;

`
import React from 'react';
import PropTypes from 'prop-types';

const Footer = (props) => {

    const {

    } = props

    return (
        <div>

        </div>
    );
};

Footer.propTypes = {

};

export default Footer;

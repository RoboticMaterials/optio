import React, {useState} from "react";

import * as styled from "./calendar_placeholder"

const CalendarPlaceholder = (props) => {
	const {

	} = props
	
	return (
		<div></div>
	)
}

export default CalendarPlaceholder